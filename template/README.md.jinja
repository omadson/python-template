# {{ package_name }}

{{ description }}

## Desenvolvimento

```bash
uv sync
uv run pre-commit install --hook-type pre-commit --hook-type commit-msg
```

Commits seguem [Conventional Commits](https://www.conventionalcommits.org/) (`fix:`, `feat:`, etc.) — versão e changelog (`docs/CHANGELOG.md`) são gerados automaticamente via python-semantic-release.
